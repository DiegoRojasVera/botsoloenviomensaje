import { CoreClass } from '@bot-whatsapp/bot/src/core/coreClass';
import { MemoryDBClass } from '@bot-whatsapp/bot/src/db';
import { LIST_ALL as EVENTS } from '@bot-whatsapp/bot/src/io/events';
import FlowClass from '@bot-whatsapp/bot/src/io/flowClass';
import { addAnswer } from '@bot-whatsapp/bot/src/io/methods/addAnswer';
import { addKeyword } from '@bot-whatsapp/bot/src/io/methods/addKeyword';
import { ProviderClass } from '@bot-whatsapp/bot/src/provider/providerClass';
import { TFlow } from '@bot-whatsapp/bot/src/types';
import * as utils from '@bot-whatsapp/bot/src/utils';
export interface GeneralArgs {
    blackList?: any[];
    listEvents?: Record<string, any>;
    delay?: number;
    globalState?: Record<string, any>;
    extensions?: any[];
    queue?: {
        timeout: number;
        concurrencyLimit: number;
    };
}
export interface BotCreationArgs {
    flow: FlowClass;
    database: MemoryDBClass;
    provider: ProviderClass;
}
/**
 * Crear instancia de clase Bot
 */
declare const createBot: ({ flow, database, provider }: BotCreationArgs, args?: GeneralArgs) => Promise<CoreClass>;
/**
 * Crear instancia de clase Io (Flow)
 */
declare const createFlow: (args: TFlow[]) => FlowClass;
/**
 * Crear instancia de clase Provider
 * Depdendiendo del Provider puedes pasar argumentos
 * Ver Documentacion
 */
declare const createProvider: <T = ProviderClass, K = any>(providerClass: new (args: K) => T, args?: K) => T;
export { createBot, createFlow, createProvider, addKeyword, addAnswer, ProviderClass, CoreClass, EVENTS, MemoryDBClass as MemoryDB, utils, };
//# sourceMappingURL=index.d.ts.map